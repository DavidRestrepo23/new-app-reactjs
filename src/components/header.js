import React from 'react';

const Header = ( ) => (
    <nav>
        <div className="nav-wrapper cyan darken-1">
            <a className="brand-logo center truncate"> Notícias Colombia </a>
        </div>
    </nav>
);

export default Header;